---
navigation:
  heading: Home
  order: 10
openerTopic:
  heading: Modelberry factory
  abstract: Sync Typescript to Contentful. Two way. Models and content.
  image: topicImage
  action:
    heading: Get started with Mbfactory
    icon: external-link
    url: https://www.npmjs.com/package/@modelberry/mbfactory

modelDataTopic:
  heading: Model your data in sync
  abstract: Data modeling is a process. As a software project grows, data models change. Mbfactory provides an easy way to keep your data models in sync with your development.
  icon: edit
  action:
    heading: Get started with Mbfactory
    icon: external-link
    url: https://www.npmjs.com/package/@modelberry/mbfactory

nodeTopic:
  heading: Node.js
  abstract: Mbfactory is available as a free Node.js package on npm.
  icon: package

tsdocTopic:
  heading: TSDoc
  abstract: Mbfactory uses TSDoc tags to annotate typescript interfaces. The same tags you use to generate documentation.
  icon: tag

opensourceTopic:
  heading: Open source
  abstract: Mbfactory is free to use and open source.
  icon: github

extendableTopic:
  heading: Extendable
  abstract: Mbfactory is structured around plugins. Easily add your own.
  icon: package

---
export { TextLayout as default } from '../layout/text-layout.jsx'
import { Topic, TopicList } from '@jaccomeijer/green-lib'
import topicImage from '../assets/images/pexels-gdtography-911758.jpg'
import { globals } from '../config/globals.js'

<div className="s-popout">
  <Topic
    className="u-margin-block-start-0"
    variant="hero"
    globals={globals}
    images={{topicImage}}
    topic={frontmatter.openerTopic}
  />
</div>

<Topic
  variant="headline"
  globals={globals}
  topic={frontmatter.modelDataTopic}
/>

---

## How does it work?

The example below shows how TSDoc tags are used to annotate typescript interfaces.

```ts
/** @modelberry
 * - {@plugin "@modelberry/plugin-contentful/plain"}
 * - {@type someModel}
 * - {@displayField title}
 * - {@description Some model description}
 * - {@name someModel}
 */
export interface ContentfulSomeModel {
  /** @modelberry {@ignore} */
  __typename?: string

  /** @modelberry
   * - {@name Title within Contentful}
   * - {@required true}
   * - {@type Symbol}
   * - {@widgetId singleLine}
   * - {@helpText Used within Contentful, will never be displayed.}
   */
  title?: string
}
```

---

<TopicList 
  className="s-grid u-grid-two-cols u-gap-m"
  variant="block"
  globals={globals}
  images={{ topicImage }}
  topics={[frontmatter.nodeTopic, frontmatter.tsdocTopic, frontmatter.opensourceTopic, frontmatter.extendableTopic]}
/>
